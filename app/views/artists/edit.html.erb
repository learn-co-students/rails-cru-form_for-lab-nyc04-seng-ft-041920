<%= form_for(@artist) do |f| %>

  <%= f.label :name %><br>
  <%= f.text_field :name %><br>
  <%= f.label :bio %><br>
  <%= f.text_area :bio %><br><br>

  <%= f.submit %>
<% end %>

<%= link_to "Back to show", artist_path(@artist) %>